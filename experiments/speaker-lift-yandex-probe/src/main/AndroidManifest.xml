<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <queries>
        <package android:name="com.byd.mediacenter" />
        <package android:name="ru.yandex.music" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <service
            android:name=".YandexSpeakerLiftAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/yandex_speaker_lift_accessibility_service" />
        </service>

        <receiver
            android:name=".ProbeCommandReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="dev.denza.speakerlift.yandexprobe.PULSE" />
                <action android:name="dev.denza.speakerlift.yandexprobe.PAUSE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
