package dev.denza.tools.runtime;

/** Included only in a qualified full runtime candidate, never in a control-only jar. */
final class CloudStartPermit {
    static final String TOKEN="native-engine-bundle-v2";
    private CloudStartPermit(){}
}
